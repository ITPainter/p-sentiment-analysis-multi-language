# 🎬 다국어 영화 리뷰 감성 분석 시스템 - Python 의존성 패키지
# 최종 업데이트: 2025년 8월 13일

# =============================================================================
# 🔧 핵심 딥러닝 및 AI 라이브러리
# =============================================================================
torch>=1.12.0                    # PyTorch - 딥러닝 프레임워크
transformers>=4.20.0            # Hugging Face - 사전 훈련 모델 및 토크나이저

# =============================================================================
# 📊 데이터 처리 및 분석
# =============================================================================
pandas>=1.5.0                   # 데이터 분석 및 조작
numpy>=1.21.0                    # 수치 계산 및 배열 처리
openpyxl>=3.0.0                 # Excel 파일 읽기/쓰기 (.xlsx)

# =============================================================================
# 🎨 데이터 시각화
# =============================================================================
matplotlib>=3.5.0              # 기본 그래프 및 차트 생성
wordcloud>=1.8.0                # 텍스트 워드클라우드 생성
pillow>=11.3.0                  # 이미지 처리 (PIL)

# =============================================================================
# 🌐 웹 및 네트워크
# =============================================================================
requests>=2.32.4                # HTTP 요청 및 API 호출
beautifulsoup4>=4.12.0          # HTML/XML 파싱 (웹 크롤링용)

# =============================================================================
# 📝 텍스트 처리
# =============================================================================
regex>=2025.7.34                # 정규표현식 (고급 패턴 매칭)
nltk>=3.7                     # 자연어 처리 도구 (선택사항)
jieba>=0.42.1                   # 중국어 토큰화
mecab-python3>=1.0.5            # 일본어 토큰화
langdetect>=1.0.9               # 언어 자동 감지
protobuf>=3.20.0                # 일본어 모델 로딩을 위해 필요
fugashi>=1.5.0                  # 일본어 토크나이저
unidic-lite>=1.0.8              # 일본어 사전

# =============================================================================
# 📋 설치 방법
# =============================================================================
# 1. 가상환경 생성 및 활성화
#    python -m venv venv
#    venv\Scripts\activate  # Windows
#    source venv/bin/activate  # Linux/Mac
#
# 2. 기본 패키지 설치
#    pip install -r requirements.txt
#
# 3. GPU 지원 (선택사항)
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 4. 추가 개발 도구 (선택사항)
#    pip install jupyter notebook ipython

# =============================================================================
# 🔍 버전 호환성
# =============================================================================
# Python: 3.8+ (권장: 3.11+)
# OS: Windows 10+, macOS 10.15+, Ubuntu 18.04+
# GPU: CUDA 11.8+ (PyTorch GPU 지원시)

# =============================================================================
# ⚠️ 주의사항
# =============================================================================
# - transformers 모델은 첫 실행시 자동 다운로드 (인터넷 연결 필요)
# - GPU 사용시 CUDA 버전 확인 필요
# - Windows 환경에서 일부 패키지 설치시 Visual C++ 빌드 도구 필요할 수 있음 