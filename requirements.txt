# 🎬 다국어 영화 리뷰 감성 분석 시스템 - Python 의존성 패키지
# 최종 업데이트: 2025년 8월 13일

# =============================================================================
# 🔧 핵심 딥러닝 및 AI 라이브러리
# =============================================================================
torch>=2.8.0                    # PyTorch - 딥러닝 프레임워크
transformers>=4.55.0            # Hugging Face - 사전 훈련 모델 및 토크나이저
tensorflow>=2.19.0              # TensorFlow - 딥러닝 프레임워크 (PyTorch와 함께 사용)

# =============================================================================
# 📊 데이터 처리 및 분석
# =============================================================================
pandas>=2.3.1                   # 데이터 분석 및 조작
numpy>=2.1.3                    # 수치 계산 및 배열 처리
openpyxl>=3.1.5                 # Excel 파일 읽기/쓰기 (.xlsx)

# =============================================================================
# 🎨 데이터 시각화
# =============================================================================
matplotlib>=3.10.5              # 기본 그래프 및 차트 생성
wordcloud>=1.9.4                # 텍스트 워드클라우드 생성
pillow>=11.3.0                  # 이미지 처리 (PIL)

# =============================================================================
# 🌐 웹 및 네트워크
# =============================================================================
requests>=2.32.4                # HTTP 요청 및 API 호출
beautifulsoup4>=4.12.0          # HTML/XML 파싱 (웹 크롤링용)

# =============================================================================
# 📝 텍스트 처리
# =============================================================================
regex>=2025.7.34                # 정규표현식 (고급 패턴 매칭)
nltk>=3.8.1                     # 자연어 처리 도구 (선택사항)

# =============================================================================
# 🗄️ 데이터베이스 및 저장
# =============================================================================
sqlite3                         # SQLite 데이터베이스 (Python 기본 내장)
json                            # JSON 처리 (Python 기본 내장)

# =============================================================================
# ⚙️ 시스템 및 유틸리티
# =============================================================================
os                              # 운영체제 인터페이스 (Python 기본 내장)
sys                             # 시스템 관련 파라미터 (Python 기본 내장)
logging                         # 로깅 시스템 (Python 기본 내장)
warnings                        # 경고 메시지 제어 (Python 기본 내장)
re                              # 정규표현식 (Python 기본 내장)

# =============================================================================
# 🎯 프로젝트별 특화 패키지
# =============================================================================
# 한국어 감성 분석 모델
# - alsgyu/sentiment-analysis-fine-tuned-model (자동 다운로드)

# 중국어 감성 분석 모델  
# - IDEA-CCNL/Erlangshen-RoBERTa-110M-Sentiment (자동 다운로드)

# =============================================================================
# 📋 설치 방법
# =============================================================================
# 1. 가상환경 생성 및 활성화
#    python -m venv venv
#    venv\Scripts\activate  # Windows
#    source venv/bin/activate  # Linux/Mac
#
# 2. 기본 패키지 설치
#    pip install -r requirements.txt
#
# 3. GPU 지원 (선택사항)
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 4. 추가 개발 도구 (선택사항)
#    pip install jupyter notebook ipython

# =============================================================================
# 🔍 버전 호환성
# =============================================================================
# Python: 3.8+ (권장: 3.11+)
# OS: Windows 10+, macOS 10.15+, Ubuntu 18.04+
# GPU: CUDA 11.8+ (PyTorch GPU 지원시)

# =============================================================================
# ⚠️ 주의사항
# =============================================================================
# - torch와 tensorflow는 동시에 설치 가능하지만 메모리 사용량 증가
# - transformers 모델은 첫 실행시 자동 다운로드 (인터넷 연결 필요)
# - GPU 사용시 CUDA 버전 확인 필요
# - Windows 환경에서 일부 패키지 설치시 Visual C++ 빌드 도구 필요할 수 있음 